# Compliance checklist

Every requirement in `docs/ehr-esignature-developer-guide.pdf` — the Definition of Done, every do
and every don't — mapped to the code that implements it and the test that proves it.

**How to read this.** A ✅ means there is code *and* a test that would fail if the behaviour were
removed. A ⚠️ means the behaviour is implemented but something about the claim is weaker than the
guide's wording, and the entry says exactly what. A 🔵 means the item is not an engineering
decision at all: it needs counsel, compliance or a purchasing decision, and no amount of code will
close it. Every ⚠️ and 🔵 is collected again in sections 6 and 7 so nobody has to hunt.

Test paths are relative to `backend/` unless they start with `frontend/`. `make check` runs all of
them: 2,163 backend tests, 147 frontend tests and 31 demo-host tests at the time of writing, plus
the two Playwright suites (14 specs against the mocks, 14 against the real stack through the demo
host) that `make check` does not run.

This document describes the code as it is, not as the spec intends it to be. Where the two differ,
the entry says so.

---

## 1. Definition of done

| # | Item | Status | Where it lives | What proves it |
|---|---|---|---|---|
| 1 | Consent disclosure is shown, explicitly accepted, and its version is stored for every signer | ✅ | `identity/consent_texts.py`; `envelopes/service.py::accept_consent`; `frontend/src/flow/steps/ConsentBlock.tsx` (under the last page of the document, in the same scroll, since Addendum 3 A) | `tests/envelopes/test_signing_flow.py::test_consent_requires_viewing_first`, `::test_consent_stores_the_text_it_was_given`, `::test_consent_to_a_stale_version_is_refused`; `tests/identity/test_consent.py::test_the_shipped_disclosure_covers_what_esign_requires` |
| 2 | Clinician signatures require re-authentication at the moment of signing | ✅ | `envelopes/service.py::_require_fresh_reauth` (`REAUTH_MAX_AGE_SECONDS`, default 120s); `create` forces `requires_reauth` for a clinician capacity whatever the template says; `documents/definitions.py` refuses to publish a clinician-capable role without it | `tests/documents/test_definitions.py::test_a_clinician_role_that_does_not_reauthenticate_is_refused`; `tests/e2e/test_happy_paths.py::test_three_signers_in_sequence_with_clinician_reauthentication`; `tests/e2e/test_isolation_and_tampering.py::test_reauthentication_cannot_be_attested_after_the_signature` |
| 3 | Guardians, witnesses and proxies are recorded in their own capacity | ✅ | `contracts.Capacity`; `signers.capacity` / `on_behalf_of`; `Actor.capacity` in the trail; the caption on the mark and the "Capacity" row on the certificate | `tests/envelopes/test_create.py::test_a_guardian_must_say_who_they_act_for`, `::test_a_guardian_acts_for_this_envelopes_patient`; `tests/documents/test_marks.py::test_on_behalf_of_appears_in_the_caption`; `tests/documents/test_certificate.py::test_every_signer_is_described_in_full` |
| 4 | The final PDF is built and flattened on the server; the client never supplies document bytes or hashes | ✅ | `documents/service.py::prepare` (flatten), `::flatten_supplied` (Addendum 2: every widget and annotation removed from a host-supplied report, nothing drawn and nothing embedded), `::apply_signer_marks`; every one of them produces its bytes through `documents/pdfutil.py::sanitized_bytes`, which re-materialises the document from its page tree so a removed object is absent rather than merely unlinked; `api/schemas.py` forbids unknown keys on every body. "The client" is still the browser: a host backend may supply a document at creation, never a signer | `tests/documents/test_prepare.py::test_a_widget_appearance_is_flattened_into_the_page`, `::test_the_prepared_revision_keeps_no_widget_object_anywhere_in_its_bytes`; `tests/documents/test_supplied.py::test_flattening_leaves_no_widget_object_anywhere_in_the_bytes`; `tests/documents/test_marks.py::test_the_signed_revision_has_nothing_interactive`; `tests/e2e/test_isolation_and_tampering.py::test_a_signer_cannot_fill_someone_elses_field_or_supply_the_date` |
| 5 | The sealed PDF validates in Adobe Acrobat, and a one-byte change makes validation fail | ⚠️ | `sealing/sealer.py` | The one-byte half is proved exhaustively: `tests/sealing/test_validate.py::test_every_single_byte_flip_in_the_first_kilobyte_is_caught`, `::test_flipping_a_byte_of_document_content_breaks_the_digest`, `::test_an_incremental_update_appended_after_the_seal_is_caught`. **Acrobat cannot be automated here.** It is a launch-checklist step in `docs/RUNBOOK.md` §1.8, and the green tick itself depends on a purchasing decision (see §7) |
| 6 | The signing key lives in KMS or an HSM, and a rotation procedure is written down | ✅ | `sealing/kms.py` — one `kms:Sign` call, no key material read, ever; `check_production_settings` refuses anything but `aws_kms` in production. Rotation: `docs/RUNBOOK.md` §2 | `tests/sealing/test_kms.py::test_seals_with_a_key_held_in_kms`, `::test_signing_happens_exactly_once_per_seal`, `::test_an_unreachable_kms_is_seal_unavailable`; `tests/api/test_http_hygiene.py::test_production_refuses_a_kms_backend_with_no_key_or_certificate` |
| 7 | The application's database role cannot update or delete audit events, and the hash chain verifies end to end | ✅ | `migrations/0002_roles.sql` (grants), `0001_schema.sql` (triggers), `0600` (truncate guards); `audit/log.py::verify` | `tests/foundation/test_roles.py::test_app_role_cannot_mutate_audit_events`, `::test_owner_update_of_an_audit_event_hits_the_trigger`, `::test_owner_truncate_of_audit_events_hits_the_trigger`; `tests/audit/test_verify.py::test_tampering_with_any_column_breaks_the_hash`; `tests/e2e/test_isolation_and_tampering.py::test_a_removed_audit_row_is_caught` |
| 8 | Sealed documents sit in write-once storage and are excluded from cleanup and deletion jobs | ✅ | `storage/s3.py` — Object Lock, conditional put, no `delete_object` anywhere; `BlobService` has no delete method | `tests/storage/test_s3_backend.py::test_an_object_is_written_under_a_compliance_lock_by_default`, `::test_object_lock_is_really_enforced_by_the_bucket`, `::test_the_backend_never_calls_delete`; `tests/storage/test_no_delete_path.py` (six tests that inspect the package for any destructive call) |
| 9 | The signer receives a copy immediately | ✅ | `GET /v1/signing/copy`; `envelopes/service.py::signer_copy`, `::may_download_copy` — the session a signer signed from stays alive for this and nothing else | `tests/envelopes/test_views_and_downloads.py::test_the_copy_is_only_ever_the_sealed_document`; `tests/envelopes/test_sign.py::test_signing_leaves_this_session_alive_for_the_copy_only`; `frontend/src/App.test.tsx` "reads, agrees, signs by typing, and gets the sealed copy -- in three screens" |
| 10 | No PHI appears in URLs, emails, text messages, logs or error tracking | ✅ | `logging.py` (allowlist); `audit/events.py` (closed models); `api/errors.py` (fixed sentences, never echoing input); ids are UUIDv4; no email or SMS code exists in the repository | `tests/e2e/test_webhooks_and_logs.py::test_no_log_line_in_a_full_run_contains_a_name_or_a_prefill_value`; `tests/foundation/test_logging.py::test_nothing_that_could_carry_phi_is_on_the_allowlist`; `tests/audit/test_no_phi.py::test_the_signer_display_name_has_nowhere_to_go`; `frontend/src/lib/api.test.ts` "never puts the token in the URL" |
| 11 | KMS and timestamp outages leave documents pending, never falsely complete, and sealing retries on recovery | ✅ | `envelopes/service.py::seal_pending` + `_on_seal_failure` (separately committed); `worker/__init__.py`; backoff 1m/5m/15m/1h/hourly | `tests/e2e/test_idempotency_and_outages.py::test_a_kms_outage_leaves_the_envelope_pending_and_two_workers_seal_it_once`, `::test_a_timestamp_authority_outage_leaves_the_envelope_pending_until_the_worker_retries`; `tests/envelopes/test_views_and_downloads.py::test_storage_being_down_leaves_the_envelope_pending_like_a_kms_outage` |
| 12 | Counsel has approved the consent wording, the certificate of completion and the document type list | 🔵 | The artefacts exist and are ready for review: `identity/consent/en-US.2026-09.txt` and `en-US.2026-10.txt`, `documents/certificate.py`, `APPROVED_DOCUMENT_TYPES` | **Not done.** No approval has been recorded. See §7 |
| 13 | An internal note explains how signatures work and how to verify one | ✅ | `docs/HOW-SIGNATURES-WORK.md`, plus `backend/src/esign/audit/README.md` for the normative hash definition | `tests/audit/test_readme_vector.py::test_the_worked_canonical_json_in_the_readme_is_byte_for_byte_correct` — the worked example is recomputed from live code, so the note cannot drift |

---

## 2. Do's and don'ts

### 2.1 Scope and templates

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO ship v1 for authenticated signers only, with a fixed set of templates | ✅ | Every entry point needs an `esk_` key or an `est_` token; templates are imported ahead of time | `tests/api/test_http_hygiene.py::test_errors_use_one_envelope_and_never_echo_input`; `tests/identity/test_hosts.py::test_every_bad_key_is_the_same_unauthorized` |
| DON'T start with email-link signing or a visual template builder | ✅ | Neither exists. `POST /v1/envelopes/bulk`, `…/email-links` and `…/documents` are explicit `422 out_of_scope` routes, so a caller gets a clear refusal rather than a 404 that looks like a bug | `tests/api/test_http_hygiene.py::test_out_of_scope_requests_are_refused_with_a_clear_code` |
| DO define fields in code, or as AcroForm fields in PDFs we author | ✅ | `templates/*.json` beside `*.pdf`, generated reproducibly by `templates/generate.py` | `tests/documents/test_sample_templates.py::test_regenerating_reproduces_the_committed_files_byte_for_byte` |
| DON'T accept arbitrary uploaded PDFs | ✅ | `inspect_template_pdf` refuses encrypted, already-signed, scripted, XFA and attachment-bearing files, and enforces size and page limits. Nothing accepts a PDF **at signing time**, and no PDF arrives from a signer or an end user at all. Since Addendum 2 a PDF may arrive from the *host backend*, over its API key, at envelope creation: `inspect_supplied_pdf` applies the same walk of the whole object graph under the supplied bounds, and adds two refusals the template path does not need — an AcroForm signature field and a visible non-widget annotation — because `flatten_supplied` removes rather than draws (`docs/SPEC-ADDENDUM-2.md`, §2.12 below) | `tests/documents/test_inspection.py::test_forbidden_features_are_refused`, `::test_an_encrypted_template_is_refused`, `::test_a_too_large_template_is_refused`; `tests/documents/test_supplied.py::test_forbidden_features_are_refused_on_a_supplied_document` (the hygiene parametrisation), `::test_a_visible_mark_drawn_as_an_annotation_is_refused_at_intake`, `::test_an_encrypted_supplied_document_is_refused`, `::test_a_supplied_document_over_the_page_limit_is_refused` |
| DO version every template; a published version is immutable | ✅ | `template_versions.status`, guarded by a database trigger, not only by application code | `tests/foundation/test_template_immutability.py` (including `::test_a_published_version_cannot_be_deleted`); `tests/api/test_templates_api.py` |
| DON'T edit a template in place once anyone has signed against it | ✅ | An envelope holds `template_version_id`; publishing is one-way; the certificate prints the key and version it was created against | `tests/envelopes/test_create.py`; `tests/documents/test_certificate.py::test_the_certificate_carries_every_field_spec_6_requires` |

### 2.2 Consent and intent

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO show the disclosure and require an explicit agree action before the first signature; store the version | ✅ | State machine refuses `SIGN` before `consented`; `consent.accepted` records id, version, locale and body hash | `tests/envelopes/test_signing_flow.py::test_consent_requires_viewing_first`; `tests/e2e/test_isolation_and_tampering.py::test_signing_before_viewing_or_consenting_is_a_conflict` |
| DON'T pre-tick the box, bury consent, or infer it | ✅ | `frontend/src/flow/steps/ConsentBlock.tsx` renders an unchecked checkbox under the document itself, and Continue is inert until it is ticked; where a standing acceptance is shown instead (§2.13) it is a statement of when this person agreed, never a pre-ticked box, and pressing Continue is still the act; the server requires `accepted: true` and a matching `consent_version` | `frontend/src/App.test.tsx` "reads, agrees, signs by typing, and gets the sealed copy -- in three screens"; `tests/envelopes/test_signing_flow.py::test_consent_to_a_stale_version_is_refused` |
| DO require a deliberate act for every signature on every document | ✅ | A signature is chosen once per session in the panel and then **applied per field by an explicit action**; one further explicit act signs the document. Since Addendum 3 A 3 that act is the press of "Sign as <name>", under the sentence "By pressing this you are signing this document", rather than a checkbox above a button that said the same thing twice: the press is what `intent_confirmed: true` means, and `sign` still requires it (C13) | `frontend/src/flow/draft.test.ts` "choosing a different signature un-applies the old one from every field", "never sends a date, an unapplied signature, or an empty optional answer"; `frontend/src/App.test.tsx` "is three taps: Continue to sign, Sign here, Sign as ..."; `frontend/e2e/signing-flow.spec.ts` "the short path is three taps" (the press sends `intent_confirmed`); `tests/e2e/test_isolation_and_tampering.py::test_a_signer_cannot_fill_someone_elses_field_or_supply_the_date` (a sign request with `intent_confirmed: false` is refused at the edge) |
| DON'T silently re-apply a saved signature | ✅ | The draft lives in memory for one session only. A signature the signer chose to *save* (Addendum 1 B) is offered back as one of three equal choices, never pre-applied: using it is still one explicit action per field, the server records `kind = adopted` and which row, and a kiosk session is never offered one | `frontend/src/flow/draft.test.ts`; `frontend/src/App.test.tsx` "a kiosk session ends on hand-back with every trace of the patient gone"; `frontend/e2e/signing-flow.spec.ts` "a saved signature is offered first, placed per field, and sent as its id"; `tests/adopted_signatures/test_offered.py`; `tests/adopted_signatures/test_saving.py::test_a_kiosk_save_is_refused_before_the_signature_is_applied`; `tests/adopted_signatures/test_revoking.py::test_a_kiosk_session_cannot_revoke_the_signers_saved_signature`; `tests/e2e/test_addendum_stories.py::test_a_kiosk_session_is_never_offered_the_patients_saved_signature` |
| DO let the signer review the full document before signing becomes available, and offer a paper alternative | ✅ | `record_viewed` checks `pages_viewed` against the page count of **the bytes that session was served**, not a number the client also supplied; decline reasons include `prefers_paper`, presented as an equally visible action | `tests/envelopes/test_signing_flow.py::test_a_second_session_cannot_claim_a_view_it_was_not_served`; `frontend/src/lib/pages-seen.test.ts` "counts a page only once it is drawn, on screen, and has stayed there"; `frontend/src/App.test.tsx` "choosing paper declines with that reason and tells the host" |
| DON'T make signing reachable before presentation, or make e-signing the only option | ✅ | As above, plus the decline path is reachable from every step before done | `frontend/src/flow/machine.test.ts` "decline can be opened from any step before done, and closed again" |
| DO give the signer their copy straight away, by download and in the portal | ✅ | `GET /v1/signing/copy` for the signer; the host files the sealed PDF from the webhook | `tests/envelopes/test_views_and_downloads.py::test_the_copy_is_only_ever_the_sealed_document`; `demo-host` files it and shows it in the chart |
| DON'T keep the signed copy visible only to staff | ✅ | Both audiences are first-class, and `document.downloaded` records which one took it (`audience: signer \| host`) | `tests/envelopes/test_views_and_downloads.py` |

### 2.3 Identity and attribution

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO bind each signature to a user ID and session | ✅ | `signers.host_user_id` → `Actor.user_id`; `RequestContext.session_id` on every event; tokens are bound to one signer and one envelope | `tests/identity/test_sessions.py::test_a_session_binds_a_token_to_one_signer`; `tests/envelopes/test_signing_flow.py::test_a_session_for_another_envelopes_signer_is_not_found` |
| DO re-authenticate clinicians at the moment of signing | ✅ | See DoD #2 | as DoD #2 |
| DON'T rely on a long-lived session for clinician attestations | ✅ | `SESSION_TTL_SECONDS` 30 min; the re-auth window is 120 s and is checked at `sign`, not at session creation; `auth_time` older than 12 h is refused outright | `tests/identity/test_reauth.py::test_an_attestation_older_than_the_window_is_refused_at_the_door`, `::test_an_attestation_that_predates_the_session_is_refused`; `tests/identity/test_sessions.py::test_auth_time_older_than_the_window_is_refused` |
| DON'T tolerate shared logins near signing | ⚠️ | The service cannot see the host's login model. What it does enforce: one token per signer, a new session revokes the previous one, `host_user_id` must be opaque and is recorded on every event, and re-authentication is an attestation the host makes per session | `tests/identity/test_sessions.py::test_a_session_binds_a_token_to_one_signer`. **The host's own account hygiene is outside this system.** Raise it in the integration review |
| DO model guardians, witnesses, interpreters and proxies explicitly | ✅ | `Capacity` is a closed literal; roles carry `allowed_capacities`; a role may be `required: false` for an optional witness or interpreter | `tests/envelopes/test_create.py::test_only_a_guardian_or_proxy_acts_on_behalf_of_someone`; `tests/envelopes/test_ordering.py::test_sequential_order_gates_session_creation` |
| DON'T let staff sign for a patient by typing the patient's name, or record a guardian's signature as the patient's | ✅ | A guardian is a signer in their own right with `capacity: guardian` and `on_behalf_of` equal to the envelope's `patient_ref`; the caption under the mark says "on behalf of", and so does the certificate | `tests/envelopes/test_create.py::test_a_guardian_acts_for_this_envelopes_patient`; `tests/documents/test_marks.py::test_the_caption_says_who_in_what_capacity_when_and_which_signer`; `tests/envelopes/test_no_phi.py::test_a_guardians_stream_names_the_patient_only_by_reference` |
| DO record which staff member started a tablet session and how identity was checked | ✅ | `KioskContext{staff_user_id, identity_check}`, recorded on `session.created` and printed on the certificate | `tests/identity/test_sessions.py::test_a_kiosk_session_records_the_staff_member_and_the_check`; `tests/envelopes/test_sealing.py::test_the_certificate_carries_the_kiosk_details` |
| DON'T attribute a kiosk signature to the logged-in staff member | ✅ | The `Actor` on `signer.signed` is derived from the signer's capacity, never from the kiosk context; the staff id lives in a separate field | `tests/api/test_sessions_api.py::test_a_kiosk_session_records_the_staff_member_and_the_check`; `tests/documents/test_certificate.py::test_every_signer_is_described_in_full` |

### 2.4 Document integrity and sealing

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO build the final PDF on the server, flatten it, then seal with PAdES (B-LT, RFC 3161) using pyHanko | ✅ | `documents/`, then `sealing/sealer.py`. `SEAL_PROFILE` defaults to `PAdES-B-LT`; production refuses `B-T` | `tests/sealing/test_seal.py::test_seal_then_validate_is_ok_for_every_profile`, `::test_long_term_profiles_embed_validation_information`, `::test_b_t_is_an_explicit_choice_not_a_downgrade` |
| DON'T write your own cryptography, byte-range handling or signature container | ✅ | pyHanko does all of it. The only hashing we do is SHA-256 over whole byte strings for the audit chain and content addressing | `sealing/` imports pyHanko throughout; there is no byte-range arithmetic in this codebase outside tests |
| DO keep the signing key in a KMS or HSM and sign through the external-signer interface | ✅ | `KmsSigner(pyhanko.sign.Signer)` — its only operation is one `kms:Sign` over the CMS signed attributes | `tests/sealing/test_kms.py::test_signing_happens_exactly_once_per_seal` |
| DON'T put a private key in the repo, env vars, an image or an app server disk | ✅ | `.dev-pki/` is git-ignored and refuses to generate under `APP_ENV=prod`; no setting holds key material, only a KMS key **id**; production refuses `SEAL_KEY_BACKEND=local` | `tests/cli/test_cli.py::test_dev_pki_refuses_to_overwrite_and_refuses_production`; `tests/api/test_http_hygiene.py::test_production_refuses_to_start_half_configured` |
| DO hash the exact bytes shown to the signer and the final sealed bytes, and store both in the trail | ✅ | `document.presented.document_sha256` is the hash of the bytes in that HTTP response; `document.sealed.document_sha256` is the sealed file. `signer.signed` additionally records the presented, base and output revision hashes | `tests/envelopes/test_signing_flow.py::test_presenting_records_what_this_session_was_shown`; `tests/verification/test_report.py::test_a_pointer_that_disagrees_with_the_revisions_is_reported` |
| DON'T trust the client: no client PDF, client hash or client-side flattening | ✅ | The Signer API accepts signature *inputs* only; bodies forbid unknown keys; `date_signed` from a client is refused with a distinct code | `tests/e2e/test_isolation_and_tampering.py::test_a_signer_cannot_fill_someone_elses_field_or_supply_the_date`, `::test_a_value_capture_cannot_claim_a_signature_kind`; `tests/documents/test_marks.py::test_a_client_supplied_date_signed_is_refused` |
| DO add each signature as an incremental update so earlier signatures stay valid (multi-signer) | ⚠️ | **Deliberate deviation, and worth being able to defend.** There is exactly one cryptographic signature, applied after the last signer. Each signer's step produces a fully hashed, stored, audit-chained revision instead. The reason is that the certificate of completion must be *inside* the sealed bytes, and appending pages after a PDF signature invalidates it — so a per-signer-signature design cannot carry the certificate the guide also requires. The per-signer evidence is the chain plus the stored revisions, and the seal covers a certificate quoting the chain's head hash, which ties them together. See `docs/HOW-SIGNATURES-WORK.md` §1 | `tests/envelopes/test_ordering.py::test_each_signer_signs_on_top_of_the_previous_revision`; `tests/verification/test_report.py::test_a_swapped_signature_image_inside_the_seal_is_reported`; `tests/envelopes/test_evidence_guards.py::test_the_certificate_quotes_the_chain_it_verified` |
| DON'T re-render, optimise, compress or re-save a PDF after any signature has been applied | ✅ | `finalize` rebuilds the document when it appends the certificate — but that happens **before any cryptographic signature exists**, and `seal` refuses an input that already carries one. After the seal, nothing touches the bytes: they are stored content-addressed and write-once | `tests/sealing/test_seal.py::test_already_signed_documents_are_refused`, `::test_seal_does_not_mutate_its_input`; `tests/verification/test_report.py::test_a_finalized_document_whose_pages_are_not_the_signed_ones_is_reported` |
| DO plan certificate rotation from day one and embed long-term validation data | ✅ | `docs/RUNBOOK.md` §2; `PAdES-B-LT` embeds a DSS, and `validate` reads it under `hard-fail` rather than ignoring it | `tests/sealing/test_seal.py::test_long_term_profiles_embed_validation_information`; `tests/sealing/test_validate.py::test_a_seal_whose_certificate_was_revoked_before_it_signed_is_refused` |
| DON'T assume a self-signed certificate earns Adobe's green tick | 🔵 | Acknowledged in code (the dev PKI is explicitly never for production) and costed in `docs/RUNBOOK.md` §1.2 | Purchasing decision. See §7 |

### 2.5 Audit trail

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO write an append-only log covering created, presented, viewed, consented, authenticated, signed, declined, voided, sealed and downloaded | ✅ | `contracts.EventType` has 28 types and covers all ten, plus `session.rejected`, `seal.failed`, `envelope.superseded`, `verification.performed`, Addendum 1's `archive.created`, `archive.attested`, `signature.adopted` and `signature.adoption_revoked`, and Addendum 2's `document.supplied` | `tests/audit/test_append.py::test_every_event_type_round_trips_through_the_database_and_still_verifies` |
| DON'T store audit events anywhere the application role can UPDATE or DELETE; grant INSERT and SELECT only | ✅ | `0002_roles.sql`, plus triggers that stop the owner role too | `tests/foundation/test_roles.py::test_app_role_has_no_update_delete_or_truncate_grant`, `::test_app_role_is_not_a_superuser_and_cannot_bypass_rls` |
| DO chain events: each row stores the previous row's hash. Use server time in UTC | ✅ | `audit/log.py`, `audit/canonical.py`; `Clock` everywhere; a clock that goes backwards on a stream is an `IntegrityFailure`, not a clamped value | `tests/audit/test_append.py::test_each_event_chains_to_the_one_before_it`, `::test_occurred_at_comes_from_the_clock_not_the_wall`, `::test_equal_timestamps_are_allowed_but_a_backwards_clock_is_refused`; `tests/audit/test_concurrency.py::test_concurrent_appends_to_one_stream_produce_a_gapless_chain` |
| DON'T use client timestamps, or plan to rebuild the trail from application logs | ✅ | No audit field is populated from client JSON except through validated, typed fields; logs are explicitly not a record (they carry no PHI and no bodies) | `tests/audit/test_no_phi.py::test_no_stored_audit_row_can_hold_free_text` |
| DO generate the certificate of completion from the log and append it before sealing | ✅ | `_certificate_summary` reads every dated fact from the trail, cross-checks the mutable rows, and refuses on a disagreement | `tests/envelopes/test_evidence_guards.py::test_the_certificate_quotes_the_chain_it_verified`, `::test_a_broken_audit_chain_refuses_to_seal`, `::test_a_certificate_never_invents_a_completion_time` |
| DON'T put more PHI in the certificate than is needed | ✅ | `CertificateSummary` cannot carry chart data: there is no field for it. The only personal data is the signers' display names, which a signature nobody can attribute would lack | `tests/documents/test_certificate.py::test_nothing_but_the_summary_reaches_the_page` |

### 2.6 Storage and retention

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO store sealed PDFs encrypted in write-once storage such as S3 Object Lock, linked to the chart | ✅ | `storage/s3.py` (Object Lock + SSE + conditional put); the host links it via `host_document_ref` and the `envelope.sealed` webhook | `tests/storage/test_s3_backend.py::test_server_side_encryption_is_on`, `::test_the_put_is_conditional_so_an_existing_key_is_never_replaced` |
| DON'T overwrite or delete a signed document; a correction is a new document that voids and references the old one | ✅ | No delete path exists. A sealed envelope is corrected by a new envelope with `supersedes_envelope_id`, which records `envelope.superseded` on the old stream. A sealed envelope can never be voided | `tests/e2e/test_endings.py::test_a_sealed_envelope_is_corrected_by_superseding_it_exactly_once`; `tests/envelopes/test_ending.py::test_a_sealed_envelope_is_never_voided` |
| DO apply the medical record retention schedule; ask compliance for the figures | ⚠️/🔵 | The mechanism is complete: `RETENTION_YEARS_BY_DOCUMENT_TYPE` per type, a 10-year default floor, passed to Object Lock as an absolute date on every blob. **The figures themselves have not been set** — the default is in force | `tests/foundation/test_clock_ids_config.py::test_retention_can_be_set_per_document_type`; `tests/storage/test_service.py::test_the_default_retention_is_the_configured_floor_measured_from_the_clock`. The table is compliance's to supply; see §7 |
| DON'T let generic cleanup jobs, TTLs or account-deletion flows touch signed records | ✅ | The only `DELETE` grant anywhere is on `idempotency_keys`, and the only deletion the code performs is of expired keys in that table | `tests/foundation/test_roles.py::test_app_role_cannot_delete_from_the_evidence_bearing_tables`, `::test_app_role_may_still_delete_expired_idempotency_keys`; `tests/storage/test_no_delete_path.py::test_the_module_never_added_a_migration_that_allows_deletion`. Operational rules are in `docs/RUNBOOK.md` §4 |

### 2.7 Security and PHI

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO authorise every document fetch | ✅ | Every host lookup is scoped to the authenticated host; every signer route is scoped by the token; another host's object is `not_found`, never `forbidden` | `tests/e2e/test_isolation_and_tampering.py::test_a_host_never_reaches_another_hosts_data`; `tests/identity/test_sessions.py::test_another_hosts_session_does_not_exist_for_reauth` |
| DO log access | ✅ | `AccessLog` middleware: method, route template, status, duration. Plus `document.downloaded` in the trail, which is the access record that matters | `tests/foundation/test_logging.py::test_a_real_log_line_carries_no_unlisted_value` |
| DO encrypt in transit and at rest | ⚠️ | At rest: S3 SSE (`AES256` or `aws:kms`) is in code and tested. In transit and database-at-rest are **deployment concerns**: TLS is terminated in front of the API (HSTS is set when `APP_ENV=prod`), and Postgres encryption is the platform's | `tests/storage/test_s3_backend.py::test_server_side_encryption_is_on`; `tests/api/test_http_hygiene.py::test_every_response_carries_the_security_headers`. Deployment items are in `docs/RUNBOOK.md` §1 |
| DON'T put PHI or guessable document IDs in URLs, emails, texts, logs or analytics | ✅ | All ids are UUIDv4; session tokens are 256-bit and never in a URL; the access log records the route template, not the path; there is no email, SMS or analytics code in the repository | `frontend/src/lib/api.test.ts` "never puts the token in the URL"; `tests/e2e/test_webhooks_and_logs.py::test_no_log_line_in_a_full_run_contains_a_name_or_a_prefill_value` |
| DO use single-use, short-lived tokens bound to one signer and one document for any signing link | ⚠️ | There is no signing *link*: the token is delivered by `postMessage` and never appears in a URL, which is stronger. It is bound to one signer and one envelope, expires in 30 minutes, is stored only as a hash, and creating a new session revokes the previous one. It is **not single-use** — the UI uses it for the whole flow and then for the copy download — so a token captured mid-flow is usable until it expires or the signer signs (which revokes every other session for them) | `tests/identity/test_sessions.py::test_a_session_binds_a_token_to_one_signer`, `::test_the_plaintext_token_never_reaches_the_database`, `::test_expired_revoked_and_unknown_are_indistinguishable`; `tests/envelopes/test_sign.py::test_signing_leaves_this_session_alive_for_the_copy_only` |
| DON'T email the document as an attachment | ✅ | Nothing in this service sends email | no email client is imported anywhere in `backend/`, `frontend/` or `demo-host/` |
| DO rate-limit and alert on the signing and re-authentication endpoints | ⚠️ | Rate limiting is implemented and tested, per host, per session and per IP, on session creation, re-auth, consent, signing, presentation, copy and verification. **Alerting is not in the code** — it is a deployment concern, with thresholds and queries in `docs/RUNBOOK.md` §3. Also note the limiter is per process (§6) | `tests/api/test_http_hygiene.py::test_session_creation_is_rate_limited_per_host_with_a_retry_hint`, `::test_guessing_tokens_is_rate_limited_per_ip`, `::test_running_a_verification_is_rate_limited_per_host`; `tests/identity/test_rate_limiter.py` |
| DON'T send documents to a third party without a BAA; a TSA receiving only a hash is fine | ✅ | The only outbound calls are: KMS (signed attributes, a few hundred bytes — no document bytes), the TSA (a hash), and the host's own webhook (ids, statuses, hashes). No PDF leaves the service except to the host and the signer | `tests/e2e/test_webhooks_and_logs.py::test_webhooks_are_signed_carry_no_phi_and_verify`; `sealing/kms.py` module docstring and `tests/sealing/test_kms.py` |

### 2.8 User experience

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO make the flow work on phones and tablets, with keyboard and screen reader support | ⚠️ | Built for it: one column, 360px up, ARIA live regions and labels throughout, focus management per step, and specific screen-reader affordances on the PDF viewer. **There is no automated accessibility audit** (no axe or equivalent in the test suite), so the claim rests on the component tests and manual review | `frontend/src/App.test.tsx` "tells a screen reader what the zoom is, and when a press changed nothing", "stays on screen while the patient reads, rather than scrolling off the top"; see §6 |
| DO offer typed and click-to-sign as well as drawn | ✅ | All three are first-class capture kinds, server and client | `tests/documents/test_marks.py::test_click_to_sign_renders_the_signers_name_in_the_plain_face`, `::test_a_typed_signature_lands_inside_its_rect`; `frontend/src/flow/draft.test.ts` "typed and click signatures are first-class capture kinds" |
| DON'T force a drawn signature | ✅ | As above. The trail records *how* each field was filled, and the wording comes from the template and the server, never the client | `tests/envelopes/test_evidence_guards.py::test_the_trail_takes_a_capture_kind_from_the_template_not_the_client` |
| DO show clear state: what is being signed, how many fields remain, and a confirmation with the copy | ✅ | Six states, a per-page progress indicator, a remaining-fields count, a review screen and a final confirmation | `frontend/src/flow/draft.test.ts` "counts what is still needed"; `frontend/src/flow/machine.test.ts`; `frontend/src/App.test.tsx` |
| DON'T allow double submission; signing must be idempotent and a retry must not create a second signature | ✅ | `Idempotency-Key` is **required** on `sign` (422 without it); a replay returns the first response; the same key with a different body is a conflict; the UI disables double submission and reuses the key on retry | `tests/e2e/test_idempotency_and_outages.py::test_a_double_submitted_signature_returns_the_same_response_and_makes_one_revision`, `::test_two_racing_submissions_with_one_key_sign_once`; `frontend/src/App.test.tsx` "retries a lost sign reply with the same Idempotency-Key and signs exactly once" |

### 2.9 Testing and failure handling

| Rule | Status | Where | Proof |
|---|---|---|---|
| DO add an automated test that flips one byte in a sealed PDF and asserts verification fails | ✅ | `tests/sealing/test_validate.py` does it exhaustively for the first kilobyte, plus content, trailer and signature-container bytes, and appended data | `::test_every_single_byte_flip_in_the_first_kilobyte_is_caught`, `::test_bytes_appended_after_the_final_eof_are_caught`, `::test_pyhanko_itself_refuses_to_append_to_our_seal`; end to end: `tests/e2e/test_isolation_and_tampering.py::test_a_tampered_sealed_blob_is_caught` |
| DO open real outputs in Adobe Acrobat and check the signature panel | ⚠️ | Cannot be automated. Launch-checklist step, `docs/RUNBOOK.md` §1.8, and a step after any change to the sealing path | See §6 |
| DON'T verify only with the library that produced the signature | ⚠️ | Partly satisfied in code: validation uses an independently built validation context, refuses to trust certificates embedded in the document, re-reads `/Location` out of the sealer's own output, and `sealed_pages_match_final_revision` compares the sealed pages against the signed revision by resolving and digesting every page resource — none of which takes the sealer's word for anything. But it is still pyHanko reading pyHanko's output. **Acrobat is the independent reader**, and it is manual | `tests/verification/test_report.py::test_a_swapped_signature_image_inside_the_seal_is_reported`, `::test_a_seal_made_for_one_envelope_does_not_verify_against_another` |
| DO test the unhappy paths: decline, void, expiry, session timeout mid-signing, second signer after first, KMS or timestamp outage | ✅ | All six, end to end | `tests/e2e/test_endings.py` (decline, void, expiry, supersede); `tests/e2e/test_idempotency_and_outages.py` (KMS and TSA outages); `tests/envelopes/test_ordering.py` (second signer, and a parallel signer whose document moved); `frontend/src/App.test.tsx` "a session that lapses mid-flow is caught wherever it happens" |
| DON'T fail open; hold as pending and retry; never show a document as complete until it is sealed and stored | ✅ | See DoD #11. `GET /signing/copy` answers `202 {"status":"sealing"}` rather than an unsealed file; the host's `document` route answers `409 not_sealed`; a seal that does not validate is discarded | `tests/envelopes/test_views_and_downloads.py::test_the_copy_is_only_ever_the_sealed_document`; `tests/envelopes/test_evidence_guards.py::test_a_broken_chain_records_the_failure_and_backs_the_job_off` |

### 2.10 Process and licensing

| Rule | Status | Where |
|---|---|---|
| DO have counsel review the consent wording, the certificate of completion and the document type list | 🔵 | Not done. §7 |
| DON'T decide for yourself which document types may be signed electronically | ✅ | `APPROVED_DOCUMENT_TYPES` is configuration, and an unapproved type cannot produce an envelope however many templates exist (`document_type_not_approved`). The list itself needs compliance's sign-off (§7) |
| DO write an internal note on how our signatures work and how to verify one | ✅ | `docs/HOW-SIGNATURES-WORK.md` |
| DON'T leave verification knowledge in one engineer's head | ✅ | The note, the CLI (`esign verify`), the API route, the button in the demo host's chart view, and a hand-verification procedure that uses none of our code |
| DO read DocuSeal, Documenso and OpenSign for ideas | — | Informational |
| DON'T copy code from them (all three are AGPL) | ✅ | No code was copied, and none of the three is a dependency. The declared dependencies in `backend/pyproject.toml` and `frontend/package.json` are permissive or LGPL — the signing stack is pyHanko (MIT), pypdf (BSD), reportlab (BSD) and Pillow (MIT-CMU) — with no AGPL package among them. A transitive licence audit is worth running once before launch |

---

### 2.11 Addendum 1 (`docs/SPEC-ADDENDUM-1.md`)

Three features, each of which states in the addendum what it weakens and how that is contained.
The summary first, then every one of those statements mapped to the code that keeps it true and the
test that would fail if it stopped being true.

| Feature | What it weakens | Status |
|---|---|---|
| **A. Paper archives** — a scan of an ink-signed document gets the same write-once storage, seal, timestamp, trail and verification | Nothing about electronic signatures: an archive has no signers and cannot claim any. What it *adds* is a document whose seal proves less than an electronic one's, which is why the claim is written on the document itself | ✅ |
| **B. Saved signatures** — a signer keeps the signature they adopted for their next session | The drawing step, and only that. Review, consent, re-authentication and an explicit apply action per field are unchanged | ✅ |
| **C. Re-authentication span** — one confirmation covers a clinician's queue | DoD #2's *per-document* proof of re-authentication | ⚠️ 🔵 |

#### A. What a paper archive's seal proves, and what it does not

| The addendum's statement | How it is contained | Where | Proof |
|---|---|---|---|
| "It proves the scan has not changed since the moment it was filed" | The scan is stored write-once as revision 1 (`scan`), hashed into `archive.created`, and the sealed bytes are cover + scan + certificate. Verification re-hashes the blob and compares the pages inside the seal against the stored scan, one page in for the cover | `archives/service.py::create`; `verification/__init__.py::_check_pointers`, `::_check_sealed_pages_match_final_revision` | `tests/archives/test_filing.py::test_the_scan_is_stored_write_once_as_revision_one`, `::test_the_sealed_document_is_cover_then_scan_then_certificate`; `tests/archives/test_verification.py::test_a_sealed_archive_verifies_completely`, `::test_a_swapped_scan_is_caught`, `::test_a_scan_whose_bytes_changed_on_disk_is_caught` |
| "…and who filed it and attested to it" | `archive.created` (actor: the host) then `archive.attested` (actor: the staff member, by opaque id, role `staff`) are the first two events; the mutable `attestation` and `paper_signed_on` columns are compared against `archive.attested` before sealing and again by verification. The opaque staff id, statement, disposition and signer *count* are compared directly; the names and the paper date — which are PHI and for an archive are the entire attribution — through `attested_detail_sha256`, one joint digest over the attesting display name, the ordered paper signers and `paper_signed_on` | `audit/events.py::attested_detail_digest`; `archives/service.py`; `envelopes/service.py::_archive_certificate_summary`; `verification/__init__.py::_check_archive_row_against_trail` | `tests/archives/test_filing.py::test_the_trail_is_the_one_the_addendum_describes`; `tests/archives/test_verification.py::test_a_rewritten_attestation_is_caught`, `::test_a_rewritten_name_or_capacity_is_caught_although_it_is_never_in_the_trail`, `::test_a_rewritten_paper_signing_date_is_caught`, `::test_the_seal_refuses_a_rewritten_attestation_rather_than_printing_it`, `::test_a_tampered_archive_audit_row_is_caught` |
| "It does not prove the ink signature is genuine… The cover page says this in plain words, and so does the certificate" | One page before the scan and the archive certificate both carry the sentence verbatim, inside the same seal; the cover still carries it when the page is at its fullest | `documents/archive_cover.py`; `documents/certificate.py::_attestation_block` | `tests/archives/test_filing.py::test_the_cover_says_what_the_seal_does_and_does_not_prove`, `::test_the_certificate_prints_the_attestation_instead_of_a_signer_table`; `tests/archives/test_cover.py::test_it_says_everything_the_addendum_asks_for`, `::test_the_most_crowded_cover_the_api_accepts_still_fits_on_the_page`, `::test_the_signers_it_cannot_fit_are_counted_and_pointed_at_the_certificate` |
| "A paper archive has no signers, no sessions and no template" | Schema CHECKs tie `kind` to template, signing order and the paper columns, and to the four states an archive may hold; the route refuses a body that looks like an electronic envelope | `migrations/0700_addendum_1.sql`; `api/archive_routes.py` | `tests/archives/test_hygiene.py::test_a_body_that_looks_like_an_electronic_envelope_is_refused`; `tests/foundation/test_migrations.py` |
| "The names are PHI: database and PDF only" (paper signers, staff member) | `archive.attested` records the opaque staff id, the closed vocabularies and a *count*; the names reach the cover page and the certificate only, and no log line or webhook | `audit/events.py::ArchiveAttestedData`; `archives/service.py` | `tests/archives/test_no_phi.py::test_no_name_reaches_audit_data`, `::test_no_log_line_from_filing_a_scan_carries_a_name`, `::test_no_name_reaches_a_webhook_payload`, `::test_the_names_are_kept_where_they_belong` |
| "The scan goes through the same hygiene as a template… `MAX_SCAN_BYTES` / `MAX_SCAN_PAGES`" | `inspect_scan_pdf` applies the template rules under the scan bounds and answers `scan_`-prefixed codes; an unapproved document type, a future signing date and a non-opaque identifier are all refused before anything is stored | `documents/inspection.py`; `archives/service.py` | `tests/archives/test_hygiene.py` (ten cases, including `::test_a_refused_scan_leaves_nothing_behind`); `tests/archives/test_cover.py::test_inspect_scan_pdf_applies_the_scan_bounds_and_scan_codes`; `tests/archives/test_filing.py::test_an_image_only_scan_is_accepted` |
| "States: `created -> completed_pending_seal -> sealed`, plus `voided` before the seal" | An archive may be voided while unsealed (nobody signed anything) and never once sealed; the electronic rule — a completed envelope waiting for its seal cannot be voided — is untouched | `envelopes/service.py::void`, `::_may_void` | `tests/archives/test_void_and_supersede.py::test_an_archive_is_voided_while_it_waits_for_its_seal`, `::test_a_sealed_archive_can_never_be_voided`, `::test_an_electronic_envelope_pending_its_seal_still_cannot_be_voided`, and the four supersede cases |

End to end: `tests/e2e/test_addendum_stories.py::test_a_paper_document_is_filed_sealed_and_verified_and_a_swapped_scan_is_caught`.
The consequence for a reader is spelled out in `docs/HOW-SIGNATURES-WORK.md` §2(g): an archive's
seal is evidence about the scan and the filing, not about the signing. Where the paper original is
destroyed after scanning, that attestation is the whole record — a records-management decision
compliance owns (C11).

#### B. Saved signatures: what stays exactly as it was

| The addendum's statement | How it is contained | Where | Proof |
|---|---|---|---|
| "Only the signer, inside their own authenticated signing session. There is no host API to upload a signature for a user" | `adopt_signature` takes a *session id*, never a user, and reads the owner from that session's signer and envelope rows. No host route creates one | `identity/adopted.py`; `api/adopted.py` | `tests/adopted_signatures/test_saving.py::test_a_host_cannot_create_a_saved_signature`; `tests/adopted_signatures/test_identity_rules.py::test_a_saved_signature_belongs_to_the_session_that_saved_it` |
| "…the row is created after the signature succeeds, in the same transaction" | A session whose signer has not signed is `signature_not_applied`; a revoked or expired session is `Unauthorized`; a replayed signature saves once | `identity/adopted.py::_session_for_adoption`; `api/signer_routes.py::post_sign` | `tests/adopted_signatures/test_identity_rules.py::test_only_a_signer_who_has_signed_may_save_one`, `::test_a_revoked_session_may_not_save_one`, `::test_an_expired_session_may_not_save_one`; `tests/adopted_signatures/test_saving.py::test_a_replayed_signature_saves_one_signature` |
| "Kiosk sessions never offer or save an adopted signature" | Refused in the identity module *and* at the route, and the session payload offers nothing on a kiosk. Three refusals for one rule: `post_sign` refuses before anything is applied (so the signature is not stamped and rolled back), `save_adopted_signature` is the backstop, and `_session_for_adoption` is the innermost. Revoking is refused from a kiosk too — it is the irreversible door, and the trail would otherwise record the signer as having asked for it | `identity/adopted.py`; `api/adopted.py::save_adopted_signature`; `api/signer_routes.py::_adopted_signature`, `::post_sign`, `::revoke_adopted_signature` | `tests/adopted_signatures/test_identity_rules.py::test_a_kiosk_session_may_not_save_one`; `tests/adopted_signatures/test_offered.py::test_a_kiosk_session_is_offered_nothing`; `tests/adopted_signatures/test_saving.py::test_a_kiosk_session_saves_nothing_and_signs_nothing`; `tests/adopted_signatures/test_applying.py::test_a_kiosk_session_cannot_apply_a_saved_signature`; `tests/e2e/test_addendum_stories.py::test_a_kiosk_session_is_never_offered_the_patients_saved_signature` |
| "The stored image is served only to a session belonging to the same `host_user_id`" | The pair is resolved from the session's rows, never from the request; another user, and the same user id at another host, are offered nothing | `api/signer_routes.py::_adopted_signature`; `identity/adopted.py::live_adopted_signature` | `tests/adopted_signatures/test_offered.py::test_a_signature_is_not_offered_to_a_different_user`, `::test_a_signature_is_not_offered_to_the_same_user_on_a_different_host`, `::test_the_saved_image_reaches_only_its_own_user` |
| "It does not remove any other step: review, consent, re-authentication and an explicit apply action per field stay exactly as they are" | The signature panel (`SignaturePanel.tsx` since Addendum 3 A 1, the adopt screen before it) offers it as one of three equal choices and never pre-applies it; the server path for an `adopted` capture is the ordinary `sign` path, which still requires viewed, consented and `intent_confirmed` | `frontend/src/flow/steps/SignaturePanel.tsx`; `frontend/src/flow/draft.ts`; `envelopes/service.py::sign` | `frontend/src/flow/draft.test.ts`; `frontend/e2e/signing-flow.spec.ts` "a saved signature is offered first, placed per field, and sent as its id"; `tests/adopted_signatures/test_applying.py::test_an_adopted_capture_stamps_the_stored_image_and_names_it_in_the_trail` |
| "The trail says which saved signature was applied… the certificate can say 'signed with a saved signature adopted on <date>'" | The capture row is `kind = adopted` with the row's id; `signer.signed` carries the digest of what was stamped and `adopted_signature_id`; the certificate prints the adoption date | `envelopes/service.py::_resolve_adopted`; `documents/certificate.py::_signer_block` | `tests/adopted_signatures/test_applying.py::test_an_adopted_capture_stamps_the_stored_image_and_names_it_in_the_trail`, `::test_an_adopted_typed_signature_carries_its_text_digest`, `::test_the_certificate_says_the_signature_was_a_saved_one`, `::test_a_signature_without_a_saved_one_records_no_adopted_id`; `tests/adopted_signatures/test_saving.py::test_the_saved_image_is_the_one_that_was_stamped` |
| "A client cannot choose what the trail says" | An `adopted` capture may carry nothing but the id — a payload beside it is refused at the API edge — and an id that is not this user's live row is `adopted_signature_unavailable`, the same answer as an unknown one | `api/schemas.py`; `envelopes/service.py::_resolve_adopted` | `tests/adopted_signatures/test_applying.py::test_an_adopted_capture_may_not_carry_a_payload`, `::test_an_unknown_saved_signature_is_refused_like_somebody_elses`, `::test_a_revoked_saved_signature_cannot_be_applied` |
| "At most one unrevoked row per `(host_id, host_user_id)`… Rows are never deleted; replacing one revokes the old row" | A partial unique index plus a per-user advisory lock; a trigger permits exactly one UPDATE, setting `revoked_at` and `revoke_reason` together, and refuses DELETE and TRUNCATE | `migrations/0700_addendum_1.sql`; `identity/adopted.py` | `tests/adopted_signatures/test_identity_rules.py::test_saving_again_leaves_exactly_one_live_row`; `tests/adopted_signatures/test_saving.py::test_saving_again_revokes_the_earlier_row`; `tests/adopted_signatures/test_racing.py::test_each_replacement_names_the_row_it_actually_revoked`; `tests/adopted_signatures/test_revoking.py::test_a_revoked_row_stays`; `tests/adopted_signatures/test_write_once.py` (the trigger, case by case); `tests/foundation/test_roles.py` (the grants, and both defences against DELETE and TRUNCATE) |
| "The host can revoke one" | Either side may: `reason: user` from the signer's own session, `reason: host` from the host API, `replaced` reserved for adoption. Idempotent, and scoped to the authenticated host, so another host's user is indistinguishable from a user with nothing saved | `api/host_routes.py`; `api/signer_routes.py`; `identity/adopted.py::revoke_adopted_signature` | `tests/adopted_signatures/test_revoking.py` (seven cases, including `::test_one_host_cannot_revoke_another_hosts_users_signature`, `::test_the_host_route_answers_the_same_for_a_user_it_does_not_have`); `tests/adopted_signatures/test_identity_rules.py::test_revoking_needs_a_reason_a_caller_may_give`; `tests/e2e/test_addendum_stories.py::test_a_host_revoke_removes_the_saved_signature_from_the_next_session` |

End to end: `tests/e2e/test_addendum_stories.py::test_a_clinician_saves_a_signature_on_one_order_and_uses_it_on_the_next`, plus `frontend/e2e/demo/addendum.spec.ts` through the real stack.
Operationally, revoking a compromised saved signature is `docs/RUNBOOK.md` §6.9.

#### C. The re-authentication span: what it weakens, and the five things that contain it

The addendum's statement is explicit: **"What this weakens. Per-document proof that the clinician
re-authenticated for that document."** It lists five containments, and one more this table adds
because the code enforces it.

| Containment, as the addendum states it | Where | Proof |
|---|---|---|
| "the span is off by default (`REAUTH_SPAN_SECONDS = 0`)" | `config.py` (`reauth_span_seconds: int = Field(default=0, ge=0, le=900)`); `identity/service.py::fresh_reauth` returns this session's own attestation or nothing when the span is zero | `tests/reauth_span/test_resolution.py::test_with_the_span_off_a_second_document_gets_nothing`; `tests/reauth_span/test_signing_queue.py::test_with_the_span_off_the_second_document_demands_its_own_attestation` |
| "capped at 900 seconds by validation" | `config.py`, `REAUTH_SPAN_MAX_SECONDS` used both as the `le=` bound and by verification: `verification/__init__.py::_check_reauth_attestations` refuses a `scope: "span"` signature whose recorded `reauth_age_seconds` exceeds the cap, so a borrowed attestation this service could never have produced is a finding years later, when the span the host was running with is long forgotten | `tests/reauth_span/test_evidence.py::test_a_span_past_the_cap_is_refused_at_startup_rather_than_clamped`, `::test_the_cap_itself_is_accepted`, `::test_a_borrowed_attestation_older_than_the_cap_is_caught` |
| "the attestation still has to be younger than `REAUTH_MAX_AGE_SECONDS` counted from `auth_time`" | `identity/service.py::fresh_reauth` takes `max(max_age_cutoff, now - span)`; `envelopes/service.py::_reauth_valid_until` shows the UI the smaller window, so it never promises a hand-off-free signature the server would refuse | `tests/reauth_span/test_resolution.py::test_a_borrowed_attestation_is_still_bounded_by_the_maximum_age`, `::test_a_borrowed_attestation_stops_at_the_end_of_the_span`; `tests/reauth_span/test_signing_queue.py::test_an_attestation_older_than_the_maximum_age_is_refused_inside_the_span`, `::test_after_the_span_has_run_out_the_second_document_demands_its_own` |
| "each document still needs its own review, consent and explicit sign action in its own session" | Unchanged: the span only answers "is there a fresh attestation?", and every other gate in `sign` stands | `tests/reauth_span/test_signing_queue.py::test_with_the_span_on_the_second_document_borrows_the_first_attestation` (each document is viewed, consented and signed on its own); `tests/e2e/test_addendum_stories.py::test_a_queue_is_confirmed_once_and_the_third_document_is_refused_after_the_span_lapses` |
| "every `signer.signed` event records which attestation was used and whether it was made in this session or borrowed… with its age at the moment of signing. The certificate prints the same" | `envelopes/service.py::sign` writes `reauth_attestation_id`, `reauth_scope`, `reauth_age_seconds`; `documents/certificate.py::_reauthentication` prints "at <time> for this document" or "at <time> in an earlier session, N seconds before signing". That block is gated on whether the signer re-authenticates at all, which `signers.requires_reauth` is a mutable copy of and no event records, so the requirement is re-derived from the immutable template role plus the capacity at signing, at sealing, when an attestation is accepted (`assert_reauth_allowed`) and again by verification rather than read off the column | `tests/reauth_span/test_evidence.py::test_the_certificate_says_which_attestation_each_signature_rests_on`, `::test_verification_passes_and_reports_the_attestation_check`, `::test_an_envelope_with_no_reauthenticating_signer_still_runs_the_check`; `tests/reauth_span/test_requires_reauth_column.py` (all four) |
| *(enforced beyond the statement)* an attestation is never borrowed across users or hosts, from a dead session, from the future, or from a row written before migration `0700`; verification re-derives all of it and catches a tampered row | `identity/service.py` (`_SPAN_ATTESTATION_SQL`); `verification/__init__.py::_check_reauth_attestations` | `tests/reauth_span/test_resolution.py::test_a_different_user_on_the_same_host_never_borrows`, `::test_the_same_user_id_on_another_host_never_borrows`, `::test_an_attestation_whose_own_session_died_is_not_borrowed`, `::test_a_row_written_before_0700_is_never_borrowed`, `::test_an_attestation_in_the_future_is_not_borrowed_either`, `::test_a_revoked_asking_session_borrows_nothing`, `::test_an_expired_asking_session_borrows_nothing`; `tests/reauth_span/test_evidence.py::test_tampering_with_the_borrowed_attestation_is_caught`; `tests/reauth_span/test_signing_queue.py::test_a_role_that_does_not_reauthenticate_is_untouched_by_the_span` |

⚠️ 🔵 because the containment is code but the *decision* is not: turning the span on is a
documented deviation from the guide's per-document rule and needs compliance's recorded agreement
(C10), with both windows set together — an attestation older than `REAUTH_MAX_AGE_SECONDS` covers
nothing, so a span alone changes nothing. The procedure, the window to choose and the query that
shows how often it was used are in `docs/RUNBOOK.md` §7.

### 2.12 Addendum 2 (`docs/SPEC-ADDENDUM-2.md`)

One feature: the host backend supplies the document itself instead of the service rendering it from
a published template. The addendum is explicit that this weakens **nothing in the evidence chain**
and changes only *provenance* — so the rows below are the statements that make that true, each
mapped to the code that keeps it true and the test that would fail if it stopped being.

| Feature | What it weakens | Status |
|---|---|---|
| **Host-supplied documents** — a per-patient report (20 to 30 pages) becomes an electronic envelope over the host API key, with its fields read out of the file or given as rects | Not the chain: revision 1 is still hashed before anyone sees it and every later step is identical. What changes is that the content came from the host rather than from a template somebody published and reviewed — which the trail and the certificate say in as many words | ✅ |

| The addendum's statement | How it is contained | Where | Proof |
|---|---|---|---|
| "The audit trail and certificate say so (`document.supplied` instead of `document.prepared`…)" | One `document.supplied` per envelope, appended in the same transaction as the envelope row, carrying the upload hash, the presented hash, the page count, the field source, the signer-role digest and the opaque `host_document_ref`. The certificate prints "Document supplied by the host" and that reference in place of the template line | `envelopes/service.py::create_from_document`; `audit/events.py::DocumentSuppliedData`; `documents/certificate.py` | `tests/envelopes/test_host_documents.py::test_document_supplied_records_both_hashes_the_page_count_and_the_reference`, `::test_the_certificate_carries_both_ends_of_the_one_transformation`; `tests/e2e/test_host_documents.py::test_a_thirty_page_report_is_signed_by_a_clinician_who_re_authenticates` |
| "The raw upload is stored too… so the transformation from upload to presented bytes is itself evidenced" | Both blobs are written write-once and content-addressed (`supplied_pdf` and `presented_pdf`) before the envelope row exists; verification re-hashes the upload and compares the presented hash across the event data, the event row, the revision and the envelope column | `envelopes/service.py::create_from_document`; `verification/__init__.py::_check_supplied_document` | `tests/verification/test_host_documents.py` (all ten cases, including `::test_a_swapped_upload_is_reported_and_revision_one_is_not_blamed`, `::test_a_missing_upload_blob_is_reported_rather_than_passed_over`, `::test_a_report_whose_bytes_are_already_stored_under_another_kind_still_verifies`) |
| "`resolve_named_fields`… with the naming rules above and correct geometry on rotated and offset-origin pages" | Widgets are claimed by name only, never by position; the rect is converted corner by corner into displayed coordinates, honouring `/Rotate` and a non-zero box origin; a role with no *signature* field is `fields_unresolved`. Field ids are built from the role key and the field type, never from the widget's name, so nothing out of a per-patient file becomes an identifier in the append-only trail | `documents/supplied.py` | `tests/documents/test_supplied.py::test_the_short_names_map_to_the_role_and_the_type`, `::test_a_widget_name_never_becomes_a_field_id`, `::test_a_rotated_page_maps_the_rect_to_what_the_reader_sees`, `::test_an_offset_box_origin_is_subtracted`, `::test_unmatched_widgets_are_dropped`, `::test_a_role_with_no_signature_field_is_refused`, `::test_the_refusal_never_echoes_a_widget_name` |
| "Explicit rects… `page` may be a positive integer or a negative one counted from the end" | `resolve_page` is the one definition of "-1 is the last page" and runs before anything is stored, so no negative page is ever persisted, printed or served; a page outside the document is `field_page_out_of_range`, and the rects are validated against the real page sizes | `contracts.py::resolve_page`; `envelopes/service.py::_resolve_supplied_fields` | `tests/documents/test_supplied.py::test_a_negative_page_counts_from_the_end`, `::test_a_page_outside_the_document_is_refused`, `::test_explicit_rects_are_validated_against_the_real_page_sizes`; `tests/e2e/test_host_documents.py::test_explicit_rects_may_count_from_the_end_of_a_report`, `::test_an_explicit_rect_off_the_end_of_the_report_is_refused` |
| "The same hygiene as templates… then flattening of any remaining form fields and annotations" | `inspect_supplied_pdf` is `inspect_template` under the supplied bounds with `supplied_`-prefixed codes, plus two refusals that follow from removing rather than drawing: an AcroForm signature field (`supplied_signature_field`) and a visible non-widget annotation carrying ink (`supplied_annotation_not_removable`). `flatten_supplied` then removes every widget, annotation and the form, draws none of them, embeds nothing, re-materialises from the page tree so nothing survives as an orphan (`pdfutil.sanitized_bytes`, the one definition of that step and the one every revision goes through), and refuses if the page count changed | `documents/service.py::inspect_supplied_pdf`, `::flatten_supplied`; `documents/inspection.py`; `documents/supplied.py::flatten_supplied`; `documents/pdfutil.py::sanitized_bytes` | `tests/documents/test_supplied.py::test_forbidden_features_are_refused_on_a_supplied_document`, `::test_a_visible_mark_drawn_as_an_annotation_is_refused_at_intake`, `::test_flattening_removes_the_widgets_and_the_form`, `::test_flattening_leaves_no_widget_object_anywhere_in_the_bytes`, `::test_flattening_keeps_the_page_content`, `::test_flattening_draws_nothing_that_was_not_already_there`, `::test_flattening_is_deterministic`, `::test_a_flattening_that_changed_the_page_count_is_refused` |
| "Approved document types apply unchanged" | The same `is_approved_document_type` gate as `create`, before any PDF work or blob write: compliance decides what may be signed electronically, whoever rendered the file | `envelopes/service.py::create_from_document` | `tests/envelopes/test_host_documents.py::test_a_document_type_outside_the_approved_list_is_refused`; `tests/e2e/test_host_documents.py::test_a_document_type_outside_the_approved_list_is_refused` |
| "`Idempotency-Key` applies (the request hash covers the document bytes)" | The stored request hash is taken over the multipart body, document included, so a replay with the same bytes returns the first response and one with different bytes is a 409 | `api/host_routes.py`; `api/idempotency.py` | `tests/e2e/test_host_documents.py::test_a_replay_with_the_same_bytes_returns_the_first_envelope`, `::test_a_replay_with_different_bytes_is_a_conflict`, `::test_a_replay_with_a_different_body_is_a_conflict_too` |
| "persist `page_count` on `document_revisions`… and use it everywhere the page count is needed" | Written when the revision is written, for every kind and both sources; `_page_count` reads the column and only falls back to parsing a blob for a pre-`0800` revision | `migrations/0800_addendum_2.sql`; `envelopes/service.py::_page_count` | `tests/envelopes/test_host_documents.py::test_the_page_count_is_persisted_on_revision_one`, `::test_a_template_envelope_persists_its_page_count_too`, `::test_the_persisted_page_count_is_the_one_presentation_reads`; `tests/e2e/test_host_documents.py::test_nothing_reparses_the_report_to_present_it`; `tests/documents/test_supplied.py::test_a_thirty_page_report_prepares_well_under_two_seconds` |
| *(enforced beyond the statement)* the roles a host document is signed by live in an UPDATE-able column, so their digest is recorded in `document.supplied` and re-checked before the signature *and* before the seal — a rewritten `requires_reauth` refuses rather than certifies | `envelopes/service.py::_require_host_document_roles_unchanged`, `::_require_definitions_match_trail`; `verification/__init__.py::_check_host_document_row_against_trail` | `tests/envelopes/test_host_documents.py::test_a_rewritten_role_is_refused_before_the_signature_not_after_it`, `::test_the_seal_refuses_roles_that_are_no_longer_the_ones_the_trail_recorded`; `tests/verification/test_host_documents.py::test_rewritten_signer_roles_are_caught_by_the_digest_in_the_trail` |
| *(enforced beyond the statement)* a role key or field id this path accepts is one the audit allowlist accepts, so no envelope can be created that could never be signed or recorded | `documents/definitions.py::ID_PATTERN` (the audit trail's own `Slug`), applied before any widget is read so named resolution cannot answer `fields_unresolved` about a key no widget name could ever match | `tests/documents/test_definitions.py::test_the_id_pattern_is_the_audit_trails_own_slug`, `::test_a_key_the_audit_trail_would_refuse_is_refused_here`; `tests/documents/test_supplied.py::test_a_role_key_that_is_not_an_id_is_refused_as_a_definition_problem`; `tests/envelopes/test_host_documents.py::test_a_role_key_that_is_not_an_id_is_refused_and_nothing_is_stored` |

End to end: `tests/e2e/test_host_documents.py` (sixteen stories, from the thirty-page report signed
with re-authentication to the two-role report signed in order, the swapped upload, the swapped
revision, and the webhooks that name no template). How a reader re-checks one of these by hand is
`docs/HOW-SIGNATURES-WORK.md` §6; what an integrator has to do differently is
`docs/INTEGRATION.md` §1b.

The one thing to be clear-eyed about: this service can prove which bytes it presented, that they
came from the upload it was given, and that nothing changed afterwards. It cannot prove the EHR
generated that upload from the right patient's record. That is the host's evidence, which is why
`host_document_ref` is recorded and printed — and why it must be opaque (C6).

### 2.13 Addendum 3 C: consent once per run (`docs/SPEC-ADDENDUM-3.md`)

The addendum's sections A and B are the signing UI's: the same acts on fewer screens, with the
server unchanged. Section C is the one that reaches this document, and its statement is explicit:
**consent to sign electronically is consent to doing business electronically, and it does not have
to be re-collected for every document in the same sitting.**

What it weakens: on the second and later documents of a sitting, the disclosure is not *displayed*
again before the signer agrees. What it does not weaken: the agreement is still recorded on every
envelope, at its own moment, against its own disclosure — `signers.consent_text_id` and
`consented_at` are set exactly as they are with the span off, and no signature is accepted without
them.

| Containment | Where | Proof |
|---|---|---|
| Off by default (`CONSENT_SPAN_SECONDS = 0`): every document collects its own consent, which is the base spec | `config.py` (`consent_span_seconds: int = Field(default=0, ge=0, le=3600)`); `envelopes/service.py::_standing_consent_for` returns `None` before it reads a row | `tests/consent_span/test_resolution.py::test_with_the_span_off_nothing_stands`, `::test_the_default_is_off`; `tests/consent_span/test_sitting.py::test_with_the_span_off_the_second_form_asks_again` |
| Capped at one hour by validation, and the cap is a property of the code rather than of a setting nobody wrote down: `verification/__init__.py::_check_consent_relied_on` refuses a gap larger than `CONSENT_SPAN_MAX_SECONDS` years later | `config.py::CONSENT_SPAN_MAX_SECONDS`, used both as the `le=` bound and by verification | `tests/consent_span/test_resolution.py::test_the_span_is_capped_at_an_hour_rather_than_clamped` |
| **The cap is on how long the disclosure may go undisplayed, not on one hop.** A document standing on the one before it, which stood on the one before that, would otherwise renew the window a document at a time while every link stayed inside the span. Each `consent.accepted` carries `relied_on_root_accepted_at` — when the notice was last actually shown — forward unchanged, and both the service and verification measure from there | `envelopes/service.py::_standing_consent_for`, `::_consent_displayed_at`; `audit/events.py::ConsentAcceptedData`; `verification/__init__.py::_check_consent_relied_on` | `tests/consent_span/test_resolution.py::test_a_chain_of_acceptances_does_not_renew_the_span_one_document_at_a_time`, `::test_a_standing_acceptance_carries_the_time_the_disclosure_was_displayed`; `tests/consent_span/test_evidence.py::test_a_chain_that_outlasts_the_maximum_span_is_caught` |
| The same person on the same host, and never anybody else: the pair is read from the signer and envelope rows, never from the request | `envelopes/repository.py::standing_consent` (`_STANDING_CONSENT_SQL`) | `tests/consent_span/test_resolution.py::test_somebody_elses_acceptance_never_stands`, `::test_the_same_person_at_another_host_never_stands`; `tests/consent_span/test_sitting.py::test_another_patients_acceptance_is_refused`, `::test_another_hosts_envelope_is_refused` |
| The same disclosure: matching is on `consent_text_id`, and `consent_texts` is unique on (version, locale), so a rolled-over version and another language both find nothing. A stale version posted anyway is still `consent_version_stale` | `envelopes/service.py::accept_consent`, `::_standing_consent_for` | `tests/consent_span/test_resolution.py::test_an_acceptance_of_a_different_disclosure_never_stands`, `::test_an_acceptance_in_another_language_never_stands`, `::test_a_stale_version_is_still_refused_as_a_stale_version` |
| Never a kiosk session, in either direction — the asking one, and the one the earlier acceptance was given from. A shared tablet is the one place "the same person is still sitting here" cannot be assumed, the same rule that keeps a saved signature off a kiosk (§2.11 B) | `envelopes/service.py::_standing_consent_for`; the `NOT EXISTS` clause in `_STANDING_CONSENT_SQL`; re-derived years later against the earlier acceptance's own signer by `verification/__init__.py::_consent_came_from_a_kiosk` | `tests/consent_span/test_evidence.py::test_a_relied_on_acceptance_given_from_a_kiosk_is_caught`; `tests/consent_span/test_resolution.py::test_a_kiosk_session_is_never_offered_standing_consent`, `::test_an_acceptance_given_on_a_kiosk_never_stands_for_a_later_document`; `tests/consent_span/test_sitting.py::test_a_kiosk_session_is_offered_nothing_and_may_claim_nothing` |
| The client never asserts it: `relies_on_envelope_id` is re-checked by the same lookup that offered the line, narrowed to the envelope named, and anything else is 409 `consent_not_standing` with the checkbox as the fallback | `api/signer_routes.py::post_consent`; `envelopes/service.py::accept_consent` | `tests/consent_span/test_sitting.py::test_an_unknown_envelope_id_is_refused_like_any_other_claim`, `::test_the_body_still_refuses_what_it_always_refused`; `tests/consent_span/test_resolution.py::test_an_envelope_never_stands_for_itself`, `::test_an_envelope_that_was_never_consented_to_does_not_stand` |
| It is on the record, not inferred: `consent.accepted` carries `relied_on_envelope_id` and `relied_on_accepted_at`, the certificate's Consented line says "(given for an earlier document in the same sitting; disclosure displayed 08:58)" — the time the notice was shown, so "earlier" is never left to mean anything up to the cap, and verification re-reads the other envelope's hash-chained trail for the acceptance | `envelopes/service.py::accept_consent`, `::_certificate_signer`; `documents/certificate.py::_consented`; `verification/__init__.py::_check_consent_relied_on` | `tests/consent_span/test_resolution.py::test_the_event_names_the_acceptance_it_relied_on`; `tests/consent_span/test_evidence.py::test_the_certificate_says_the_agreement_was_given_earlier_in_the_sitting`, `::test_the_certificate_says_when_the_disclosure_was_displayed`, `::test_verification_passes_and_reports_the_consent_check`, `::test_a_relied_on_acceptance_that_is_not_in_that_trail_is_caught` (three tampers), `::test_a_relied_on_envelope_that_is_gone_is_caught` |
| A client that has never heard of any of this is unaffected: a consent POST without the field behaves exactly as before, whatever the span is set to | `api/schemas.py::ConsentBody`; `envelopes/service.py::accept_consent` | `tests/consent_span/test_resolution.py::test_consent_without_the_field_behaves_exactly_as_before`; `tests/consent_span/test_sitting.py::test_a_client_that_sends_nothing_new_is_unaffected` |

End to end: `tests/e2e/test_addendum_stories.py::test_a_patient_reads_the_disclosure_once_and_signs_three_forms`.
How a reader follows one of these acceptances back to the document that displayed the notice,
without running any of our code, is `docs/HOW-SIGNATURES-WORK.md` §7(j).

⚠️ 🔵 because the containment is code but the *decision* is not: turning the span on means a signer
agrees on a notice they read a few minutes earlier, which needs compliance's recorded agreement
(C12), exactly as the re-authentication span does (C10).

### 2.14 Addendum 3 A and B: three screens, and a run through a queue

Five screens became three (read, sign, done) and a clinician working a queue signs each document
in about three taps. **Nothing that produces evidence was removed**: what went is a checkbox that
restated the button under it, a summary screen that repeated what the signer had just done, and a
separate screen for choosing a signature. Each act the record rests on is still an act.

| What has to stay true | Where it is now | Proof |
|---|---|---|
| Every page can be displayed before signing, and the server is told when they have been | Unchanged: `POST /signing/viewed` is sent when the last page has been displayed, from the Read screen; `record_viewed` still checks the count against the bytes that session was served | `frontend/src/lib/pages-seen.test.ts`; `frontend/e2e/demo/reports.spec.ts` "the clinician reads all thirty pages…" (the gate holds on thirty pages: there is no Continue to sign until every page has been displayed, only "Next unseen page (n)" and a mark per page in the bar -- addendum 3 F; `frontend/src/lib/reading.test.ts` proves the button reaches every page a fling skipped); `tests/envelopes/test_signing_flow.py::test_a_second_session_cannot_claim_a_view_it_was_not_served` |
| Consent is given explicitly, and the disclosure is there to read | `ConsentBlock.tsx`, directly under the last page in the same scroll — collapsed to its opening lines with "Read the full notice" expanding in place, so the document it is about is still on screen. `POST /signing/consent` is sent when Continue is pressed, as before | `frontend/src/App.test.tsx` "reads, agrees, signs by typing, and gets the sealed copy -- in three screens"; `frontend/e2e/signing-flow.spec.ts` (the notice opens and closes in place; Continue is inert and says which of the two is missing) |
| One explicit act per field | The fields list on the Sign screen: a row per field with the part of the page its mark lands on, and one button, tick box or text box each. Applying is still per field and still explicit | `frontend/src/flow/draft.test.ts`; `frontend/src/App.test.tsx` "a multi-field document: one explicit act each, optional fields skippable", "a field can be undone, and the count and the sign button follow" |
| One explicit act that signs the document | The press of "Sign as &lt;name&gt;", under the sentence that says what pressing it does. It carries `intent_confirmed: true`, which the API still requires (§2.2, C13). A guardian's button names the person they act for, and names them in words: the host may send `on_behalf_of_display` beside the opaque `on_behalf_of` the record keeps, so the sentence standing for a parent's intent is one they can read | `frontend/src/App.test.tsx` "is three taps: Continue to sign, Sign here, Sign as ..."; `frontend/e2e/demo/addendum.spec.ts` (the three presses counted in a real browser against the real service); `tests/envelopes/test_create.py::test_a_guardian_is_shown_the_patients_name_while_the_trail_keeps_the_reference` |
| Re-authentication for the roles that need it | Asked for by that press rather than on a screen of its own: the UI posts `esign:reauth_required`, waits inline, and sends the signature itself on `esign:reauth_done` — no second tap. Every timeout, refusal and lapse is rendered on the same screen. The server's rule is untouched: `sign` refuses without a fresh attestation | `frontend/src/App.test.tsx` "hands off, waits, believes the server, and then signs without another tap", "gives up waiting after a while…", "a confirmation that lapses between the screen and the signature asks once more"; `frontend/e2e/demo/signing.spec.ts` "the clinician re-authenticates through the EHR on the press" |
| A decline-to-paper path visible on every screen before done | One quiet link in the footer of both Read and Sign | `frontend/src/App.test.tsx` "the paper path is reachable from Read and from Sign"; `frontend/src/flow/machine.test.ts` "decline can be opened from any step before done, and closed again" |
| A queue is the host's, not the UI's | `esign:init` may carry `queue {index, total, next_title}`; the UI shows the position, counts down on Done and posts `esign:next {envelope_id}`. It never fetches the next document, never holds a second token, and a position the host could not have meant is dropped rather than shown | `frontend/src/lib/embed.test.ts`; `frontend/src/App.test.tsx` "shows the position, counts down, and asks the host for the next document", "stays put when asked to…", "ignores a position the host could not have meant"; `demo-host/tests/test_demo_host.py::test_what_the_frame_says_it_finished_is_checked_against_what_this_host_opened` |

The one thing for counsel is the intent confirmation: it is now the button press rather than a
checkbox above it (C13). Everything else on this list is the same act in a different place.

## 3. Audit event minimum fields (guide §6)

Every field the guide asks for, with the column that holds it. The full definition — canonical
encoding and a worked test vector — is in `docs/HOW-SIGNATURES-WORK.md` §3 and
`backend/src/esign/audit/README.md`.

| Guide field | Our column | Notes |
|---|---|---|
| `event_id`, `sequence` | `id`, `sequence` | Monotonic per stream, gapless, serialised by an advisory lock |
| `document_id`, `template_id`, `template_version` | `stream_id`; `envelope.created.data.template_key` / `template_version` / `template_version_id` | The stream *is* the document; the template identity is on the creation event, and verification checks the row still agrees with it |
| `event_type` | `event_type` | 28 types, covering all ten the guide names |
| `actor_user_id`, `actor_role`, `capacity`, `on_behalf_of` | `actor_user_id`, `actor_role`, `actor_capacity`, `on_behalf_of` | All four are top-level hashed columns |
| `auth_method` | `auth_method`, plus `session.created.data.auth_method` and `auth_time` | |
| `occurred_at` | `occurred_at` | Server `Clock`, UTC, six fractional digits, never from a client |
| `ip_address`, `user_agent` | `ip`, `user_agent` | Server-side, honouring `TRUSTED_PROXY_CIDRS`; the user agent is truncated to 512 characters *before* hashing |
| `document_sha256` | `document_sha256` | The presented bytes, or the sealed bytes on `document.sealed` |
| `consent_text_version` | `consent.accepted.data.consent_version` | With the text id, the locale and the body hash |
| `prev_event_hash`, `event_hash` | `prev_event_hash`, `event_hash` | `SHA-256` over the canonical JSON of the other seventeen columns |

`tests/audit/test_append.py::test_the_hashed_field_list_is_exactly_the_stored_columns_minus_the_hash`
proves nothing can be added to the table without entering the hash.

---

## 4. Stop and escalate (guide §5)

These are not engineering decisions. The service refuses each of them rather than approximating.

| Case | How it is handled |
|---|---|
| **Controlled-substance prescriptions** | Out of scope by design. Not an approved document type, and no template exists. A DEA-compliant EPCS flow needs a certified application and two-factor signing; we integrate a certified vendor and never build it |
| **Witness or notary requirements** | Witnesses *are* modelled (capacity `witness`, sequential ordering, the procedure-consent sample uses one). Whether a witness is legally sufficient for a given document type in a given state is 🔵 counsel's call, per document type and per state. Notarisation is out of scope |
| **Non-US signers** | Out of scope. eIDAS may require an advanced or qualified signature, which needs a trust service provider. Nothing here claims to produce one |
| **New document types** | 🔵 Compliance first, always. Enforced mechanically: `APPROVED_DOCUMENT_TYPES` gates envelope creation (`document_type_not_approved`), so adding a template is not enough |

---

## 5. What the code does beyond the guide

Not requirements, but worth knowing when someone asks how defensible the record is:

- **Verification is itself recorded.** `verification.performed` carries the outcome, so the trail
  shows who checked and what they found.
- **The certificate of completion is built from the trail, not the rows**, and a disagreement
  between the two stops the seal (`certificate_evidence_mismatch`) rather than being certified.
- **The seal is bound to its envelope** (`/Location = envelope:<id>`), and both the sealer and the
  verifier read it back.
- **The certificate's head hash is checked twice**: against the chain as it stands, and by looking
  for it in the text of the sealed pages.
- **Signing refuses a revision the signer has not seen** — not just "has not viewed *something*",
  but "the bytes you read are still the current revision" (409 `not_viewed`).
- **The raw ink is evidence too.** The drawn PNG and the typed text are hashed into `signer.signed`
  and stored append-only, so a swapped signature image is a finding, not a silent substitution.
- **Refusals are recorded.** `session.rejected` and `seal.failed` are written in their own committed
  transactions, because the failing request's transaction is rolled back and the refusal still
  matters.
- **Out-of-scope requests get a named refusal** rather than a 404 that looks like a bug.
- **A weakening is printed on the document that carries it.** A signature that rested on a borrowed
  re-authentication says so on the certificate, in words, with the age; a paper archive's cover page
  states what its seal does *not* prove. Neither fact is left in a configuration file for somebody
  to reconstruct years later (§2.11).

---

## 6. Gaps, honestly

Everything marked ⚠️ above, in one place, with what would close it. A struck-through row is one
that was open when this list was written and has since been closed; it stays, with the tests that
closed it, because "this was once missing" is worth knowing.

| # | Gap | Impact | To close |
|---|---|---|---|
| G1 | **No Adobe Acrobat validation in CI.** DoD #5 and the "don't verify only with the library that produced the signature" rule both want an independent reader. Our validator is careful and independent *in construction*, but it is still pyHanko | Medium. A pyHanko-specific bug could produce a file that we accept and Acrobat rejects — the worst possible failure, because it surfaces in front of a patient's lawyer | Manual step in the launch checklist and after any change to `sealing/`. A CI job could at least add a second independent implementation; there is no headless Acrobat |
| G2 | **The rate limiter is per process.** `SlidingWindowRateLimiter` holds counters in memory and does not share them between replicas, so N API replicas allow N times the documented limit | Low–medium. It exists to slow guessing, not to meter traffic, and a 256-bit token is not guessable at any of these rates | A shared limiter (Redis), or an edge rate limiter in front of the service. Noted in `docs/RUNBOOK.md` §7 |
| G3 | **No alerting ships with the service.** The guide says "rate-limit **and alert** on the signing and re-authentication endpoints" | Medium. A seal backlog that nobody watches is a silent outage: signatures are taken and nothing completes | Wire the queries and thresholds in `docs/RUNBOOK.md` §3 into your monitoring. The signals exist (structured logs, `seal_jobs`, `seal.failed` events, `/healthz`) |
| G4 | **No automated accessibility audit.** ARIA and focus management are present and covered by component tests, but nothing runs axe or equivalent | Medium. A patient on a tablet in a clinic is exactly the user who suffers, and this is a flow people cannot skip | Add `@axe-core/playwright` to the existing Playwright run, and one manual screen-reader pass per release |
| G5 | **Session tokens are not single-use.** Short-lived (30 min), bound to one signer and one envelope, never in a URL, hashed at rest, revoked by a new session and by signing — but usable for the whole flow | Low. The guide's rule is aimed at emailed signing links, which do not exist here | Nothing, unless the threat model changes. It is documented rather than quietly ticked |
| G6 | **Retention figures are not set.** The mechanism is complete; `RETENTION_YEARS_BY_DOCUMENT_TYPE` is empty and the 10-year default floor applies to everything | High, and it is a **compliance** gap rather than a code one. Minors' records often need "until majority plus N years", which is longer than 10 and not expressible as a fixed number of years from signing | Compliance supplies the table; set the configuration. If a duration cannot be expressed as years-from-signing, that needs a design conversation before launch, not after |
| G7 | **Encryption in transit and database-at-rest are deployment concerns**, not code | Low, provided the runbook is followed | `docs/RUNBOOK.md` §1. Verify at deployment, not by reading this repository |
| G8 | **Shared logins at the host** are outside what this service can see | Medium, and it is the EHR's to answer | Raise in the integration review with each host. The service records `host_user_id` on every event, so a shared account is at least *visible* in the trail afterwards |
| G9 | **`esign` has no subcommand for rotating a webhook secret or disabling a host.** `rotate_webhook_secret` and `disable_host` exist in `esign.identity` and are tested, but reaching them in an incident means a Python one-liner | Low, but it is friction at exactly the wrong moment | Add `esign hosts rotate-webhook-secret` and `esign hosts disable`. The one-liners are in `docs/RUNBOOK.md` §6.7 in the meantime |
| G10 | **`backend/src/esign/sealing/README.md` is out of date** in its last "Known limits" bullet: it says `SealValidation.ok` "is computed from four booleans and ignores `problems`". That stopped being true when `ok` gained `and not self.problems` (SPEC §13, second round), and `sealer.py`'s own comment says so | Low, but it is a documentation defect in exactly the file a reviewer reads to decide whether the validator fails closed — and it understates the code | One-line correction to that bullet. Flagged rather than changed here because that file is the sealing module's, not this documentation set's |
| G13 | **Adding a field to an audit event's `data` makes every earlier event of that type report a verification failure.** `audit/log.py::verify` compares each row's key set against the model the running build declares, so Addendum 1's four new `signer.signed` keys mean a document signed before migration `0700` now verifies `FAILED` on `audit_chain` (`data keys do not match signer.signed at 7`) and on `reauth_attestations_match_trail`, although every hash, the seal and `certificate_head_hash` still pass. Addendum 3 added three more, to `consent.accepted` (`relied_on_envelope_id`, `relied_on_accepted_at`, `relied_on_root_accepted_at`), so every envelope consented to before that release now reports a second such line whether or not `CONSENT_SPAN_SECONDS` was ever turned on — the same benign cause, one release later, which is the argument for fixing it rather than documenting it a third time and the hand re-check in `docs/HOW-SIGNATURES-WORK.md` §2(e) reports `problems: none`. Confirmed on a real pre-`0700` envelope | Medium, and it is about **triage**, not evidence: nothing stored is wrong, but the weekly sample verification and `docs/RUNBOOK.md` §6.5 both read "chain problem" as tampering, and a `FAILED` on a sealed document is the worst thing to have to explain twice | Either version the event models (verify a row against the model in force when it was written) or record the schema version on the row. Until then it is documented on both sides: §6.5 step 0 tells on-call how to tell it apart in one query, the upgrade procedure says to keep a pre-upgrade `esign verify --json` baseline, and `docs/HOW-SIGNATURES-WORK.md` §4 says what the sub-problem means |
| ~~G11~~ | ~~**The 900-second cap on `REAUTH_SPAN_SECONDS` has no test.**~~ **Closed.** The bound is now the named constant `esign.config.REAUTH_SPAN_MAX_SECONDS`, used both as the `le=` on the field and by `verification/__init__.py::_check_reauth_attestations`, which reports a `scope: "span"` signature claiming a borrowed attestation older than the cap | — | `tests/reauth_span/test_evidence.py::test_a_span_past_the_cap_is_refused_at_startup_rather_than_clamped`, `::test_the_cap_itself_is_accepted`, `::test_a_borrowed_attestation_older_than_the_cap_is_caught` |
| ~~G12~~ | ~~**The `adopted_signatures` write-once rule has no direct test.**~~ **Closed.** `adopted_signatures` is now in `NO_DELETE_TABLES` and in `APPEND_ONLY_ROW_MAKERS`, so it gets the same treatment as every other evidence table: the app role's grants asserted against the catalogue, its `DELETE` refused with `42501`, and the owner role's `DELETE` and `TRUNCATE` refused by the triggers. The shape of the one permitted UPDATE has its own file | — | `tests/foundation/test_roles.py` (`::test_app_role_may_read_write_and_update_but_never_truncate`, `::test_app_role_cannot_delete_from_the_evidence_bearing_tables`, `::test_owner_cannot_delete_from_the_other_append_only_tables`, `::test_both_defences_hold_against_truncating_the_other_append_only_tables`); `tests/adopted_signatures/test_write_once.py` (revocation without a reason, a revocation that also edits another column, a second revocation, un-revoking, owner `DELETE`) |

None of these is a correctness bug in the evidence path — G13 reports a failure where there is no
defect, which is the opposite mistake and still a bad one. G6 is the one that should block launch.

---

## 7. For counsel and compliance, not for code

No engineering work will close any of these. Each needs a named owner and a recorded decision
before launch.

| # | Item | Owner | Why it cannot be code |
|---|---|---|---|
| C1 | **Approve the ESIGN disclosure wording.** `identity/consent/en-US.2026-09.txt` and `en-US.2026-10.txt`. Consent texts are immutable and versioned, so an approved version stays exactly as approved, and a new one is a new version | Counsel | It is the legal text that makes consent effective |
| C2 | **Approve the certificate of completion.** What it says, what it prints about each signer, and the "how to verify" paragraph. It travels with the document wherever the document goes | Counsel | It is the page a court reads |
| C3 | **Approve `APPROVED_DOCUMENT_TYPES`.** Today: `patient_consent`, `hipaa_acknowledgement`, `procedure_consent`, `clinical_order`. The list also decides which document types may be filed as paper archives | Compliance | The guide is explicit that engineers do not decide which document types may be signed electronically |
| C4 | **Supply the retention schedule** per document type, including the rule for minors (G6) | Compliance | Varies by state and by document type |
| C5 | **Rule on witnessing and notarisation** per document type and per state. The service models a witness; whether that is sufficient is a legal question | Counsel | State law |
| C6 | **Decide the certificate authority**, including whether to buy an AATL-member certificate so Acrobat shows a green tick rather than "validity unknown" to an outside reader. Options and costs are in `docs/RUNBOOK.md` §1.2 | Team lead + procurement | A purchasing decision the guide explicitly says to escalate |
| C7 | **Confirm no BAA is needed with the timestamp authority.** A TSA receives a hash and nothing else; the guide says this is fine, but the vendor should still be on the record | Compliance | Vendor management |
| C8 | **Confirm the identity-proofing story for kiosk sessions.** The service records which staff member started the session and how identity was checked (`photo_id`, `dob_and_name`, `known_to_staff`, `wristband`). Whether those methods are acceptable for a given document type is a policy question | Compliance | Clinic policy |
| C9 | **Accept the one-seal-at-the-end design** (§2.4). It is a deliberate, documented deviation from the guide's "incremental update per signature" wording, taken so the certificate of completion can be inside the sealed bytes. Counsel should be comfortable with the explanation before the first dispute, not during it | Counsel + engineering | A judgement about what is defensible |
| C10 | **Decide whether a re-authentication span is acceptable, and for how long** (`REAUTH_SPAN_SECONDS`, §2.11 C). The service ships with it off: one re-authentication per document, as the guide asks. Turning it on lets a clinician sign a queue on one confirmation, with every signature recording which confirmation, whether it was borrowed and how old it was, and the certificate printing the same in words; whether that trade is acceptable, and the window, is a policy decision. **Both windows are set together** — `REAUTH_MAX_AGE_SECONDS` bounds the span, so raising the span alone changes nothing. The procedure and the audit query are in `docs/RUNBOOK.md` §7 | Compliance | The guide's per-document rule is explicit, and this is a documented deviation from it |
| C11 | **Rule on the records treatment of scanned paper originals** (§2.11 A). Filing a scan (`POST /v1/archives`) gives the *scan* write-once storage, a seal, a timestamp and a trail for the document type's retention period. It says nothing about the paper: the host states `retained`, `returned_to_signer` or `destroyed_per_policy`, and the service records and prints that without judging it. Whether an original may be destroyed once scanned — and which document types that is true for — is a state records-management question, and it should be answered before staff start sending `destroyed_per_policy`. Where the original is gone, the scan plus one staff member's attestation is the entire record of an ink signature | Compliance + counsel | State records law and clinic policy; no code can decide it, and the seal deliberately does not claim the ink is genuine |
| C12 | **Decide whether consent may stand for a sitting, and for how long** (`CONSENT_SPAN_SECONDS`, §2.13). The service ships with it off: the disclosure is displayed on every document. Turning it on means a patient signing three forms at one desk reads it once and the later forms show "You agreed to sign electronically at 09:12" instead of the checkbox — each of them still recording its own acceptance, naming the one it stood on, printing it on the certificate and re-checking it at verification. ESIGN consent is consent to doing business electronically rather than to one form, so this is defensible; whether it is acceptable here, and the window, is a policy decision. Never available on a shared tablet | Compliance + counsel | A documented deviation from "display the disclosure before each agreement", contained the same way C10 is |
| C13 | **Accept the button press as the intent confirmation** (§2.14, Addendum 3 A 3). Until Addendum 3 the signer ticked "I want to sign it as …" and then pressed "Sign document"; now one primary button says "Sign as &lt;name&gt;", under the sentence "By pressing this you are signing this document. It counts the same as signing on paper, and you will get a copy." The request still carries `intent_confirmed: true` and the service still refuses without it — what changed is that the press, rather than a box above it, is what that flag means. ESIGN asks for an act done with the intent to sign, not for a particular widget, so this is defensible; counsel should say so on the record before the first dispute | Counsel | A judgement about what a court will read as intent, which no test can make |

---

## 8. Re-checking this document

It is prose about code, so it can drift. When it matters:

```sh
make check                                    # the gate: every test cited here
cd backend && uv run pytest tests/e2e -q      # the end-to-end evidence tests
cd backend && uv run esign verify <id>        # a real document, end to end

# every test this document names still exists (a renamed test is the usual drift)
grep -hoE '::test_[a-z0-9_]+' docs/*.md README.md | sed 's/^:://' | sort -u > /tmp/cited
grep -rhoE 'def test_[a-z0-9_]+' backend/tests demo-host | sed 's/^def //' | sort -u > /tmp/defined
comm -23 /tmp/cited /tmp/defined              # prints nothing when the citations are good
```

The single most valuable recurring check is the weekly sample verification in `docs/RUNBOOK.md` §7:
five sealed envelopes, `esign verify` each. It exercises the seal, the blob store, the chain and
the certificate together, and it is how a slow corruption gets found before somebody in a
deposition finds it.
